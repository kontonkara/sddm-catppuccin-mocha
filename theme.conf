[General]
# Username field mode: "editable" or "locked"
UsernameMode=locked

# Background settings
# Type: "color", "image", "video"
BackgroundType=color

# For BackgroundType=image or video, specify path
# BackgroundPath=background.jpg
# BackgroundPath=background.gif
# BackgroundPath=background.mp4

# Background blur (0-100, only for images)
BackgroundBlur=0

# Background dim (0.0-1.0, 1.0 = no dim, 0.0 = fully black)
BackgroundDim=1.0
